# Travel Record

## 프로젝트

- Flutter/Dart 앱입니다. 초기 구현은 작고 플랫폼 공통으로 유지합니다.
- 패키지를 추가하기 전에 기존 Flutter와 Dart API를 우선 사용합니다.
- 기능상 필요할 때만 UI, 상태, 데이터 모델, 저장소를 분리합니다. 구체적인 사용처가 하나뿐인 추상화는 만들지 않습니다.
- 소스 코드, 로그, 스크린샷, 문서에 비밀번호, API 키, 개인정보를 노출하지 않습니다.
- 기능에 실제로 필요한 플랫폼 권한만 요청합니다.

## 작업 방식

- 코드를 변경하기 전에 기존 구현을 확인하고 현재 패턴을 재사용합니다.
- Dart 코드를 변경한 뒤 `flutter analyze`와 가장 관련 있는 최소 테스트를 실행합니다.
- 동작이 변경되면 `docs/` 아래 관련 문서를 함께 갱신합니다.
- 변경 범위를 작게 유지하고, Flutter가 안전하게 재생성할 수 있는 생성 파일만 정리합니다.

## 반드시 지켜야 할 규칙

- 모든 대화는 한국어로만 진행할 것
- 지시하지 않은 작업은 수행하지 말것
- 오버엔지니어링 금지

## 명령어

```text
flutter pub get
flutter analyze
flutter test
flutter run
```

## 행동 지침 (Behavioral Guidelines)

> 출처: https://github.com/multica-ai/andrej-karpathy-skills/blob/main/CLAUDE.md

LLM의 흔한 코딩 실수를 줄이기 위한 행동 지침. 위의 프로젝트 지침과 함께 적용한다.

**Tradeoff:** 이 지침들은 속도보다 신중함에 무게를 둔다. 사소한 작업에는 판단에 맡긴다.

### 1. Think Before Coding

**추측하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전에:
- 가정을 명시적으로 말한다. 불확실하면 질문한다.
- 해석이 여러 가지라면 모두 제시한다 — 조용히 하나를 고르지 않는다.
- 더 단순한 접근이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 불명확한 점이 있으면 멈춘다. 무엇이 혼란스러운지 지목하고 질문한다.

### 2. Simplicity First

**문제를 푸는 최소한의 코드. 추측성 코드는 금지.**

- 요청받지 않은 기능은 만들지 않는다.
- 한 번만 쓰이는 코드에 추상화를 만들지 않는다.
- 요청되지 않은 "유연성"이나 "설정 가능성"을 넣지 않는다.
- 발생할 수 없는 시나리오에 대한 에러 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 가능하다면 다시 쓴다.

스스로에게 묻는다: "시니어 엔지니어가 이걸 과하다고 할까?" 그렇다면 단순화한다.

### 3. Surgical Changes

**꼭 필요한 것만 건드린다. 자기가 만든 것만 치운다.**

기존 코드를 수정할 때:
- 인접한 코드·주석·서식을 "개선"하지 않는다.
- 고장나지 않은 것을 리팩터링하지 않는다.
- 자기 취향과 다르더라도 기존 스타일을 따른다.
- 무관한 죽은 코드를 발견하면 언급만 한다 — 삭제하지 않는다.

변경으로 고아 코드가 생겼을 때:
- 내 변경 때문에 쓰이지 않게 된 import·변수·함수는 제거한다.
- 원래부터 죽어 있던 코드는 지시 없이 제거하지 않는다.

판단 기준: 변경된 모든 줄이 사용자의 요청으로 직접 추적되어야 한다.

### 4. Goal-Driven Execution

**성공 기준을 정의하고, 검증될 때까지 반복한다.**

작업을 검증 가능한 목표로 변환한다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 작성하고, 통과시킨다"
- "버그 수정" → "버그를 재현하는 테스트를 작성하고, 통과시킨다"
- "X 리팩터링" → "리팩터링 전후로 테스트가 통과함을 보장한다"

여러 단계가 필요한 작업은 간단한 계획을 먼저 말한다:
```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]
```

강한 성공 기준은 독립적인 반복을 가능하게 한다. 약한 기준("동작하게 만들어라")은 끊임없는 재확인을 요구한다.

**이 지침이 작동하고 있다는 신호:** diff에 불필요한 변경이 줄어들고, 과도한 복잡성 때문에 다시 쓰는 일이 줄어들며, 실수 후가 아니라 구현 전에 확인 질문이 나온다.

