# Travel Record

## 프로젝트

- Flutter/Dart 앱입니다. 초기 구현은 작고 플랫폼 공통으로 유지합니다.
- 패키지를 추가하기 전에 기존 Flutter와 Dart API를 우선 사용합니다.
- 기능상 필요할 때만 UI, 상태, 데이터 모델, 저장소를 분리합니다. 구체적인 사용처가 하나뿐인 추상화는 만들지 않습니다.
- 소스 코드, 로그, 스크린샷, 문서에 비밀번호, API 키, 개인정보를 노출하지 않습니다.
- 기능에 실제로 필요한 플랫폼 권한만 요청합니다.

## 작업 방식

- 코드를 변경하기 전에 기존 구현을 확인하고 현재 패턴을 재사용합니다.
- Dart 코드를 변경한 뒤 `flutter analyze`와 가장 관련 있는 최소 테스트를 실행합니다.
- 동작이 변경되면 `docs/` 아래 관련 문서를 함께 갱신합니다.
- 변경 범위를 작게 유지하고, Flutter가 안전하게 재생성할 수 있는 생성 파일만 정리합니다.

## 반드시 지켜야 할 규칙

- 사용자의 인가없이 기존 소스를 임의로 수정하지 않는다.

## 명령어

```text
flutter pub get
flutter analyze
flutter test
flutter run
```
