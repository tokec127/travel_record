# 여행기록 앱 실행 계획

## 변경 이력

| 버전 | 날짜 | 작성(수정)자 | 변경 내용 |
|---|---|---|---|
| v1.0 | 2026-09-23 | tokec | 분야별 WBS와 실행 계획 작성 |
| v1.1 | 2026-09-23 | tokec | Android P0 기반 구현 상태 기록 |
| v1.2 | 2026-09-23 | tokec | 현재 Flutter Android 구현과 검증 결과 반영 |
| v1.3 | 2026-09-23 | tokec | Firebase 토큰 검증·Firestore 백엔드 API 구현 상태 반영 |
| v1.4 | 2026-09-25 | tokec | Android 카카오지도 경로·마커 표시 완료와 Flutter 원격 기능 미연결 상태 반영 |

## 1. 계획 기준

- 대상 문서: `docs/1~9`의 개발 규칙, 도메인, PRD, 사용자 시나리오, 와이어프레임, 구조 원칙, 기술 다이어그램, ERD
- 개발 인원: 1명
- 핵심 기능 목표: 2일 이내
- 플랫폼 우선순위: Android와 iOS
- 공통 기술: Flutter + Dart
- 저장 전략: 로컬 우선. DBMS와 서버 데이터 저장 범위는 별도 결정
- 완료 조건은 실제 동작 확인과 관련 테스트 통과를 기준으로 한다.

### 현재 구현 상태

- 완료: Flutter 앱 골격, Android 로컬 인증, 10분 백그라운드 복귀 재인증, 여행 생성·로컬 저장, 5분 기본 GPS 자동 수집, 사진 기간 grid·메타데이터 관리, WWIS 날씨 저장, Android Kakao 지도 상태별 중심·축척, 네이티브 날짜별 폴리라인·기록 마커, 지도 핀치·드래그·현재 위치 이동
- 부분 완료: 지도는 현재 Android 카카오지도만 구현했으며, 해외 Google Map 전환과 iOS 경로 폴리라인은 후속 과제다. 독립 메모·결제·검색·동영상·폴더 파일 첨부도 후속 범위다.
- 미완료: Flutter 클라이언트의 Firebase 인증·백엔드 API 연결, Google Drive·SNS·주변 기기 실제 전송, 배터리 한도 설정의 실제 적용, iOS 실기기 검증
- 검증 완료: `dart analyze lib/main.dart`, `flutter test test/widget_test.dart`, Android debug APK 빌드가 통과했다. 전체 `flutter analyze`에는 vendored Kakao 예제의 누락 의존성 오류가 남아 있다.

## 2. 우선순위와 의존성

| 우선순위 | 범위 | 목표 |
|---|---|---|
| P0 | 앱 실행, 인증, 여행 생성, 로컬 저장, 경로 조회 기본 흐름 | 2일 내 동작하는 핵심 흐름 |
| P1 | 과거여행, 메모, 활동 상세, 설정, 권한 오류 | 핵심 흐름의 사용성 확보 |
| P2 | Firebase 확장, 외부 공유, 주변 기기 공유, 자동 수집 확장 | P0·P1 검증 후 진행 |

```text
C-01 범위 확정
  ├─ F-01 앱 기반 구조 ─ F-02 인증 ─ F-03 여행·로컬 저장
  │                                  └─ F-04 경로·활동 조회
  ├─ A-01 Android 기반 ─ A-02 Android 위치·미디어
  └─ I-01 iOS 기반 ─ I-02 iOS 위치·미디어

F-03/F-04 ─ F-05 메모·과거여행·설정
F-05 ─ B-01 Firebase 인증 확장 ─ B-02 공유·동시 접속 검증
D-01 저장 방식 결정 ─ D-02 스키마·마이그레이션(선택)
```

## 3. 공통·프로젝트 관리 작업

### C-01 범위와 결정사항 확정

선행 Task: 없음

수행 작업:

- P0에 로컬 인증, 여행 생성, 로컬 저장, 기본 경로 조회를 포함한다.
- P1·P2 기능의 구현 시점을 분리한다.
- 현지 지도 제공자를 국내 카카오지도, 국외 Google Map으로 확정한다.
- 할 일 목록은 제품 범위 확정 전 제안 화면으로 유지한다.
- DBMS와 서버 데이터 저장 범위를 미정 상태로 기록한다.

완료 조건:

- [ ] P0·P1·P2 범위가 PRD와 일치한다.
- [ ] 미정 항목이 임의의 구현 요구사항으로 바뀌지 않았다.
- [ ] 변경 결정이 관련 문서의 변경 이력에 기록되었다.

### C-02 Flutter 프로젝트 기반 점검

선행 Task: C-01

수행 작업:

- 현재 Flutter 프로젝트와 실행 대상 플랫폼을 확인한다.
- 필요한 Flutter·Dart 버전과 기존 의존성을 확인한다.
- 앱 시작점, 분석 설정, 테스트 실행 상태를 확인한다.

완료 조건:

- [ ] `flutter pub get`이 성공한다.
- [ ] `flutter analyze`의 기존 오류 원인을 확인한다.
- [ ] 기본 `flutter test` 실행 결과를 기록한다.

### C-03 통합 검증과 문서 갱신

선행 Task: F-04, A-02, I-02

수행 작업:

- P0 사용자 시나리오를 처음부터 끝까지 실행한다.
- Android와 iOS의 차이와 미완료 기능을 기록한다.
- 코드·화면·데이터 변경에 맞춰 관련 문서를 갱신한다.

완료 조건:

- [ ] `flutter analyze`가 통과한다.
- [ ] 관련 위젯 또는 통합 테스트가 통과한다.
- [ ] Android 실기기 또는 에뮬레이터에서 P0 흐름을 확인한다.
- [ ] iOS 실기기 또는 시뮬레이터에서 P0 흐름을 확인한다.
- [ ] 문서 변경 이력이 누락되지 않았다.

## 4. Frontend 작업

### F-01 앱 골격과 공통 UI

선행 Task: C-02

수행 작업:

- `lib/app`에 앱 시작, 라우팅, 테마를 구성한다.
- 와이어프레임의 색상, 글꼴, 간격, 터치 영역을 공통 스타일로 정의한다.
- 인증, 여행, 경로, 설정 화면의 빈 화면 경로를 만든다.
- 반응형 기준 폭 320px·390px·600px 이상을 확인한다.

완료 조건:

- [x] 앱이 인증 화면에서 시작한다.
- [ ] 공통 앱 바와 버튼 스타일이 화면마다 일관된다.
- [ ] 작은 화면에서 텍스트와 버튼이 잘리지 않는다.

### F-02 로컬·원격 인증 화면

선행 Task: F-01, A-01, I-01

수행 작업:

- 로컬 잠금 해제 상태를 확인한다.
- 생체인증·기기 PIN·비밀번호 흐름을 플랫폼 API 경계로 연결한다.
- Firebase Authentication 로그인 상태를 화면 상태와 연결한다.
- Android Google·이메일 링크, iOS Apple·Google·이메일 링크 진입점을 둔다.
- 인증 실패·취소·오프라인 상태를 표시한다.

완료 조건:

- [x] 로컬 인증 성공 시 자료 화면에 접근한다.
- [x] 로컬 인증 실패 시 자료가 노출되지 않는다.
- [ ] 원격 인증 성공 시 Firebase `uid`를 확인한다.
- [ ] 인증 취소와 실패에 재시도 경로가 있다.
- [ ] 토큰과 비밀번호가 로그나 일반 설정에 노출되지 않는다.

### F-03 여행 생성과 로컬 저장

선행 Task: F-01, F-02, D-01

수행 작업:

- 국내 도시·해외 나라 선택과 시작일·종료일 입력 화면을 구현한다.
- 여행 식별자, 지역 유형, 현지 시간대, 상태를 저장한다.
- 여행 목록에서 진행 중·과거 여행을 구분한다.
- 저장 실패와 잘못된 날짜를 표시한다.

완료 조건:

- [ ] 국내 도시와 해외 나라를 각각 생성할 수 있다.
- [ ] 종료일이 시작일보다 빠른 입력을 거부한다.
- [x] 앱 재실행 후 저장한 여행을 조회한다.
- [ ] 여행 자료가 사용자 `uid`와 연결된다.

### F-04 경로·활동 조회

선행 Task: F-03, A-02, I-02

수행 작업:

- 현지 지도에서 날짜별 GPS 경로를 표시한다.
- 국내 카카오지도와 국외 Google Map 선택 경계를 연결한다.
- 활동 기록을 자료 종류별 아이콘으로 표시한다.
- 아이콘 선택 시 사진·동영상·메모·결제·날씨 상세 팝업을 표시한다.
- 위치가 없거나 경로가 비어 있는 상태를 표시한다.

완료 조건:

- [ ] 여행과 날짜를 선택하면 해당 경로만 표시된다. (현재 상세 화면은 저장된 여행 전체 경로를 날짜별 색상으로 표시)
- [ ] 활동 아이콘과 상세 팝업이 자료 종류와 일치한다.
- [ ] 빈 경로와 낮은 위치 정확도를 사용자에게 표시한다.
- [x] 지도 서비스 오류가 로컬 자료 조회를 막지 않는다.

### F-05 메모·과거여행·설정·로그

선행 Task: F-03, F-04

수행 작업:

- 제목 기본값, 내용, 저장·수정, 첨부, 배경색·글꼴 설정을 구현한다.
- 과거여행 목록, 전체 기간 경로, 날짜별 경로 선택, 수정·삭제를 구현한다.
- 위치 수집 간격, 배터리 한도, 외부 연동 설정 화면을 구현한다.
- 디버그 모드 전용 로그 화면을 구현한다.

완료 조건:

- [ ] 제목이 없을 때 현지 날짜·시간이 표시된다.
- [x] 사진첩의 여행기간 사진을 grid로 조회하고 메타데이터를 저장·숨김 처리한다.
- [ ] 과거여행 전체 경로와 날짜별 경로를 구분한다.
- [ ] 삭제 전 확인하고 삭제 결과를 표시한다.
- [ ] 로그에 토큰·비밀번호·원본 메시지가 없다.

## 5. Backend 작업

### B-01 Firebase Authentication 및 백엔드 API

선행 Task: C-01, F-01

수행 작업:

- Firebase 프로젝트와 Android·iOS 앱 식별 정보를 준비한다.
- Flutter Firebase Authentication 연동을 구성한다.
- 플랫폼별 Google·Apple·이메일 링크 인증 설정을 확인한다.
- Firebase `uid`를 앱 사용자와 여행 데이터의 소유권 기준으로 사용한다.
- `backend/` Node.js API에서 Firebase ID 토큰을 검증하고 Firestore 사용자 경로를 사용한다.
- 여행, GPS, 사진 메타데이터, 공유 세션 REST API를 제공한다.

완료 조건:

- [ ] Android에서 Google 또는 이메일 링크 인증을 확인한다.
- [ ] iOS에서 Apple·Google 또는 이메일 링크 인증을 확인한다.
- [ ] 인증 실패·취소·계정 연결 오류를 처리한다.
- [x] 인증 설정값과 토큰이 저장소에 노출되지 않는다.
- [x] 백엔드 도메인 검증 테스트가 통과한다.

### B-02 원격 데이터와 공유 범위 결정

선행 Task: C-01, D-01

수행 작업:

- Firebase를 인증 외 데이터 저장에 사용할지 결정한다.
- 로컬 우선 자료와 원격 동기화 자료의 범위를 정한다.
- Google Drive·SNS·주변 기기 공유의 데이터 포맷과 권한을 정한다.
- 공유 동의, 전송 상태, 재시도, 대용량 미디어 처리 방식을 정한다.

완료 조건:

- [ ] 원격 저장 대상과 로컬 전용 대상이 문서화되어 있다.
- [ ] 공유 전 사용자 확인 범위가 정의되어 있다.
- [ ] 주변 기기 수신 동의 전송 차단이 정의되어 있다.
- [ ] 실패·중단·재시도 상태가 정의되어 있다.

### B-03 동시 접속 검증

선행 Task: B-01, B-02

수행 작업:

- 인증과 공유의 서버 요청 범위를 측정한다.
- 1,000명 동시 접속 시나리오와 목표 응답 시간을 정한다.
- 부하 테스트 환경과 테스트 데이터를 준비한다.
- 병목과 비용을 기록하고 필요할 때만 구조를 확장한다.

완료 조건:

- [ ] 1,000명 동시 접속 부하 테스트가 재현 가능하다.
- [ ] 인증·공유 주요 요청의 성공률과 응답 시간이 기록된다.
- [ ] 병목에 대한 최소 개선안과 보류 항목이 구분된다.

## 6. DB 작업

현재 DBMS가 확정되지 않아 DB 구축은 P0에서 수행하지 않는다.

### D-01 저장 방식 결정

선행 Task: C-01

수행 작업:

- 로컬 파일·메타데이터 저장 방식과 암호화 요구를 결정한다.
- Firebase를 인증 외 데이터 저장에 사용할지 결정한다.
- ERD의 `USER`, `TRIP`, `ROUTE`, `LOCATION_POINT`, `ACTIVITY`, 상세 자료, 공유 세션을 실제 저장 대상으로 매핑한다.
- 대용량 미디어의 실제 파일과 메타데이터 보존 위치를 결정한다.

완료 조건:

- [ ] 저장 대상과 저장 위치가 기능별로 정의된다.
- [ ] 사용자 `uid` 소유권과 삭제 정책이 정의된다.
- [ ] DBMS 또는 Firebase 저장 사용 여부가 결정된다.
- [ ] 결정 전에는 `schema.sql`을 생성하지 않는다.

### D-02 스키마와 DDL

선행 Task: D-01

수행 작업:

- 결정된 DBMS에 맞춰 ERD를 물리 모델로 변환한다.
- 기본키·외래키·인덱스·날짜·좌표·상태 값을 정의한다.
- 마이그레이션과 기존 자료 보존 경로를 작성한다.
- DBA 협의용 `docs/schema.sql`을 생성한다.

완료 조건:

- [ ] DDL이 선택한 DBMS에서 실행된다.
- [ ] 사용자별 접근 제한을 데이터 계층에서 검증한다.
- [ ] 마이그레이션과 롤백 방법이 문서화된다.
- [ ] ERD와 DDL의 테이블·관계가 일치한다.

## 7. Android 플랫폼 작업

### A-01 Android 프로젝트와 권한 기반

선행 Task: C-02

수행 작업:

- Kotlin·Gradle과 지원 SDK를 확인한다.
- 위치, 미디어, 로컬 인증, 주변 기기 공유에 필요한 권한을 기능별로 정리한다.
- `AndroidManifest.xml`에 실제 사용하는 권한만 선언한다.
- Flutter 플러그인으로 가능한 기능과 Kotlin 브리지 필요 기능을 구분한다.

완료 조건:

- [x] 디버그·릴리스 빌드의 권한 선언이 확인된다.
- [ ] 권한 거부·철회·제한 상태가 정의된다.
- [ ] 플랫폼 채널이 필요한 기능과 데이터 형식이 문서화된다.

### A-02 Android 위치·미디어·로컬 인증

선행 Task: A-01, F-01

수행 작업:

- 백그라운드 위치 수집과 배터리 제한을 구현한다.
- 사진·동영상 메타데이터 접근을 구현한다.
- 생체인증과 기기 PIN·비밀번호 대체 흐름을 연결한다.
- 프로세스 종료·재시작·권한 변경 후 상태를 복구한다.

완료 조건:

- [x] 설정된 위치 간격으로 위치 기록이 생성된다.
- [ ] 백그라운드 제한과 배터리 상태를 사용자에게 표시한다.
- [x] 위치·미디어 권한 없이 자료가 노출되지 않는다.
- [ ] 앱 재시작 후 중복·유실 상태를 확인한다.

### A-03 Android 공유와 기기 검증

선행 Task: A-02, B-02

수행 작업:

- Google Drive·SNS 공유 호출 경계를 연결한다.
- Wi-Fi·Bluetooth 주변 기기 검색과 동일 앱 확인을 구현한다.
- 수신 동의 후에만 전송한다.
- Android 실기기에서 연결 끊김과 재시도를 확인한다.

완료 조건:

- [ ] 수신 동의 전 데이터가 전송되지 않는다.
- [ ] 동일 앱이 없는 기기를 공유 대상으로 확정하지 않는다.
- [ ] 연결 실패와 전송 완료 상태가 표시된다.

## 8. iOS 플랫폼 작업

### I-01 iOS 프로젝트와 권한 기반

선행 Task: C-02

수행 작업:

- Swift·Xcode와 지원 iOS 버전을 확인한다.
- `Info.plist` 권한 설명과 백그라운드 capability를 기능별로 정리한다.
- 위치, 사진, 로컬 인증, 주변 기기 공유에 필요한 권한만 선언한다.
- Flutter 플러그인으로 가능한 기능과 Swift 브리지 필요 기능을 구분한다.

완료 조건:

- [ ] `Info.plist`의 권한 설명이 실제 기능과 일치한다.
- [ ] 백그라운드 모드가 필요한 기능만 활성화된다.
- [ ] 권한 거부·제한 상태의 화면 흐름이 정의된다.

### I-02 iOS 위치·미디어·로컬 인증

선행 Task: I-01, F-01

수행 작업:

- 백그라운드 위치 수집과 앱 생명주기 중단·재개를 처리한다.
- 제한된 사진 선택과 미디어 메타데이터 접근을 처리한다.
- Face ID·Touch ID와 기기 암호 대체 흐름을 연결한다.
- Keychain 사용이 필요한 인증·연동 정보의 저장 경계를 정한다.

완료 조건:

- [ ] 설정된 위치 간격으로 위치 기록이 생성된다.
- [ ] 백그라운드 실행 제한을 사용자에게 안내한다.
- [ ] 사진 접근 제한 상태를 처리한다.
- [ ] 앱 생명주기 전환 후 중복·유실 상태를 확인한다.

### I-03 iOS 공유와 기기 검증

선행 Task: I-02, B-02

수행 작업:

- Apple·Google·이메일 인증 설정을 실기기에서 확인한다.
- Google Drive·SNS 공유와 iOS 공유 시트 경계를 연결한다.
- 주변 기기 검색·수신 동의·전송 흐름을 검증한다.
- iOS 실기기에서 백그라운드·권한·공유 실패를 확인한다.

완료 조건:

- [ ] Apple 로그인이 취소·실패될 때 복구 경로가 있다.
- [ ] 공유 시트에서 선택한 여행 데이터만 전달된다.
- [ ] 수신 동의 전 데이터가 전송되지 않는다.
- [ ] iOS 실기기에서 핵심 흐름을 확인한다.

## 9. 2일 실행 순서

### 1일차: P0 기반

1. C-01 범위와 미정사항을 확정한다.
2. C-02 프로젝트 기반을 점검한다.
3. F-01 앱 골격을 만든다.
4. A-01·I-01 플랫폼 권한 기반을 준비한다.
5. F-02 로컬 인증과 원격 인증 기본 화면을 연결한다.
6. F-03 여행 생성과 로컬 저장을 구현한다.

### 2일차: P0 검증과 P1 착수

1. A-02·I-02 위치와 미디어의 최소 수집을 연결한다.
2. F-04 날짜별 경로와 활동 조회를 구현한다.
3. F-05 메모 또는 과거여행 중 우선순위가 높은 기능을 구현한다.
4. C-03 Android·iOS P0 흐름을 검증한다.
5. 남은 기능은 P1·P2로 기록하고 무리하게 확장하지 않는다.

## 10. 계획 완료 기준

- [ ] 모든 분야 Task에 선행 Task와 완료 조건이 있다.
- [ ] P0와 P1·P2가 분리되어 있다.
- [ ] DBMS 미정 상태에서 불필요한 DDL을 만들지 않는다.
- [ ] Android와 iOS 플랫폼 차이가 별도 Task로 관리된다.
- [ ] 1,000명 동시 접속 목표가 별도 부하 테스트 Task로 관리된다.
- [ ] 구현 완료 후 문서·테스트·실기기 검증을 수행한다.
